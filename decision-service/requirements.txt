# 圆衡 Enkei 决策服务依赖（Python 3.11+）
# 由安装器在 decision-service\.venv 中安装，永不使用开发机的绝对路径 venv。
# tradingagents 为包内 vendored 上游开源组件（Apache-2.0），见 THIRD_PARTY_NOTICES。

# 本地 vendored 上游引擎：TradingAgents 0.4.0
-e ./vendor/tradingagents

# 决策服务本体
fastapi>=0.115
uvicorn>=0.30
jsonschema>=4.20
httpx>=0.27
python-dotenv>=1.0

# 历史数据仓与研究回测模块（research\）
pandas>=2.3
matplotlib>=3.8
tabulate>=0.9
