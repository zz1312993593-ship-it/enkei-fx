# 貢献ガイド（Contribution Guide）

円衡 FX（Enkei FX）への貢献を歓迎します。このリポジトリは「ローカル優先・フェイルクローズド」の設計原則を守るため、以下のルールを設けています。

## 基本原則（変更提案はすべてこれに照らして審査されます）

1. **偽装しない**：データ不足・検証失敗時は待機表示や拒否で応答すること。デモデータ・架空の履歴・推測値で結果を埋めない。
2. **実取引の境界を動かさない**：実取引経路は既定でグローバルロック。自動解除・自動有効化・隠れた注文経路を追加する変更は受け付けません。
3. **秘密は保存しない**：API キー・口座情報・ペアリングコードをログ・監査・フロントエンド・テストに含めない。
4. **判断は監査可能に**：AI の出力は品質ゲートを通し、監査記録を残す。
5. **再現可能に**：バックテスト・モンテカルロは決定論的（固定シード）であること。

## 開発環境

- Windows 10+ / Node.js 22.13+ / Python 3.11+
- `npm install` → `npm run ci`（lint / tsc / build / テスト / 監査）
- Python テスト: `ai-terminal/.venv/Scripts/python.exe -B -m unittest discover -p "test_*.py"`

## Pull Request のルール

- 一つの PR は一つのトピック。変更理由・検証方法（実際のコマンド出力）・未検証の範囲を説明に書く。
- 新しい依存関係を追加する場合はライセンス（MIT / BSD / Apache-2.0 系）と必要性を明示。
- UI 変更は日本語・英語・中国語の三言語対応を確認（`t(中, 日, 英)` ヘルパーを使用、英語の省略＝中国語フォールバックは禁止）。
- 戦略ロジックの変更はバックテスト＋サンプル外＋モンテカルロの結果を添付。

## 戦略の取り込みについて

MQL5 マーケットや第三者 EA の**コードをコピーすることはできません**（ライセンス上の理由）。公開された考え方・手法だけを参考に、独自実装として追加し、研究ゲートで検証してください。手順は [docs/strategy-intake.ja.md](./docs/strategy-intake.ja.md) を参照。

## 言語

Issue・PR は日本語 / English いずれでも可です。リポジトリ内の `docs/` にある中国語の設計・引き継ぎ文書は開発履歴の記録であり、ユーザー向けドキュメントではありません。
